# zramcfg
```
Usage of zramcfg:
  -b, --block string      ZRAM block device to use (default "zram0")
  -e, --enable            Enable ZRAM
  -d, --disable           Disable ZRAM
  -s, --size int          ZRAM size in bytes
  -c, --compalgo string   Compression algorithm to use
```
